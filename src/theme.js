import {
  Dimensions
} from 'react-native'

const dimensions = Dimensions.get('window')

const colors = {
  pink: '#f50856',
  blue: '#00bbd3',
  yellow: '#ffc400'
}

const fonts = {
  regular: 'regular-font',
  bold: 'regular-font',
  italic: 'italic-font'
}

export {
  colors,
  dimensions,
  fonts
}